# Backend base URL (axios). Ví dụ: http://localhost:8080 — không có slash cuối hoặc có đều được
VITE_API_URL=

# true = luôn dùng mock | false = luôn HTTP | để trống = có VITE_API_URL thì gọi API, không thì mock
VITE_USE_MOCK_API=
